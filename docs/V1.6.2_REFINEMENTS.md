# BatteryKeeper v1.6.2

- 桌面长按只保留：系统电池、导入截图。
- 首页充电时显示预计充满时间：Android 系统 ETA 优先，无法获取时才使用容量与当前电流估算。
- 功率显示统一为 2 位小数。
- mAh 展示统一为 1 位小数；不修改 Room 数据库字段类型，避免老用户升级迁移风险。
- 当前电量只有在 Android `EXTRA_LEVEL / EXTRA_SCALE` 真正提供小数精度时才显示 2 位小数，否则继续整数显示。
- 包含 v1.6.1 `Row/Column weight` 错误导入修复。
